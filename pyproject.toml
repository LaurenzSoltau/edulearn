[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "edulearn"
version = "0.9.0"
description = "education machine learning library"
requires-python = ">=3.11"

dependencies = [
    "numpy"
]

[project.optional-dependencies]
dev = [
    "pytest",
]
