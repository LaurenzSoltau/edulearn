# edulearn

A small ML library built from scratch with numpy, mostly to understand the algorithms
instead of just calling scikit-learn. Each method gets a short write-up of the math in
`docs/` and then a straightforward implementation.

Very early, nothing really works yet.
